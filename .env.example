# Local run can work without MongoDB. In that case runtime CSV files are used.
# For real online deployment, use MongoDB Atlas connection string.
MONGO_URI=
MONGO_DB=adaptive_captcha

# Optional. Used by uvicorn locally or Render automatically.
PORT=8000
