services:
  - type: web
    name: adaptive-captcha-api
    env: docker
    plan: free
    autoDeploy: true
    envVars:
      - key: MONGO_DB
        value: adaptive_captcha
      - key: NODE_ENV
        value: production
